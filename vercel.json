{
  "buildCommand": "prisma generate && prisma migrate deploy && npx tsx prisma/seed.ts && next build",
  "env": {
    "NODE_ENV": "production"
  },
  "git": {
    "silent": false
  },
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- src/ scripts/ prisma/ public/ package.json pnpm-lock.yaml"
}
