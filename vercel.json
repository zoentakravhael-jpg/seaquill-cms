{
  "buildCommand": "prisma generate && next build",
  "env": {
    "NODE_ENV": "production"
  },
  "git": {
    "silent": false
  },
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- src/ scripts/ prisma/ public/ package.json pnpm-lock.yaml",
  "installCommand": "pnpm install && pnpm exec prisma migrate deploy"
}
